name: Local CD Demo

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Deploy backend and frontend locally
        run: |
          cd /srv/cicd-app
          git pull origin main
          echo "Deployed at $(date)" > deployed.txt
